import 'package:flutter/material.dart';

class ColorPallete {
  final Color bgColor = const Color.fromRGBO(245, 247, 248, 1);

  final Color mainColor = const Color.fromRGBO(71, 169, 146, 1);

  final Color secondaryColor = const Color.fromRGBO(69, 71, 75, 1);

  final Color borderColor = const Color.fromRGBO(52, 51, 67, 1);

  final Color errorColor = Colors.redAccent;
}
